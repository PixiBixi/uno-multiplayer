node_modules/
dist/
coverage/
*.tsbuildinfo

# Design docs and plans are prose. Prettier would reflow the fenced code
# samples they contain, so they stay outside its scope.
docs/
playwright-report/
test-results/

# Cocogitto owns this file and needs the literal `- - -` separator to find where to
# insert a release. Prettier normalises it to `---`, after which `cog bump` fails
# with "cannot find default separator" - which is exactly how this line came to be.
CHANGELOG.md
